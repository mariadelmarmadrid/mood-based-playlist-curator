<?xml version="1.0" encoding="utf-8"?><!--
  Mood list screen
  - Static top app bar with a filter action
  - Slide-down filter card anchored under the app bar
  - RecyclerView with day “cards” below
  - Bottom navigation + center FAB
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sand_50">

    <!-- ===== Top App Bar (fixed) =====
         Hosts title + filter menu (menu_top)
    -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/sand_50"
        android:elevation="8dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:title="@string/app_name"
            app:menu="@menu/menu_top"
            app:navigationIcon="@null"
            app:titleCentered="false"
            app:titleTextColor="@color/colorTextPrimary" />

        <!-- Thin divider to suggest a static bar (no scroll) -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#1A000000" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- ===== Filter Card (initially hidden) =====
         Anchored under the app bar; shown/hidden with short slide animation
    -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/filterCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:elevation="8dp"
        android:visibility="gone"
        app:cardCornerRadius="16dp"
        app:cardElevation="6dp"
        app:layout_anchor="@id/appBar"
        app:layout_anchorGravity="bottom">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <!-- Search notes (single-line TextInputLayout) -->
            <com.google.android.material.textfield.TextInputLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/search_notes_hint">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/inputSearchNotes"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:imeOptions="actionSearch"
                    android:singleLine="true" />
            </com.google.android.material.textfield.TextInputLayout>

            <!-- Date range presets (single selection) -->
            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipsDate"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                app:singleSelection="true">

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipAllDates"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checkable="true"
                    android:checked="true"
                    android:text="@string/chip_all_dates" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipToday"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checkable="true"
                    android:text="@string/chip_today" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipLast7"
                    style="@style/Widget.Material3.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checkable="true"
                    android:text="@string/chip_last7" />
            </com.google.android.material.chip.ChipGroup>

            <!-- Min daily average slider (−2..2 step 1) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:orientation="vertical">

                <!-- Label updated live from code -->
                <TextView
                    android:id="@+id/avgLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/min_daily_avg_label"
                    android:textAppearance="@style/TextAppearance.Material3.BodyMedium" />

                <com.google.android.material.slider.Slider
                    android:id="@+id/sliderMinAvg"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:valueFrom="-2"
                    android:valueTo="2"
                    android:stepSize="1"
                    android:value="-2"
                    android:contentDescription="@string/mood_neutral" />
            </LinearLayout>

            <!-- Filter actions (Reset/Apply) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="end"
                android:orientation="horizontal">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnReset"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/button_reset"
                    android:textColor="@color/colorAccent"
                    app:iconTint="@color/colorAccent"
                    app:rippleColor="@color/colorAccent"
                    app:strokeColor="@color/colorAccent"
                    app:strokeWidth="1dp" />

                <!-- Primary Apply action -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnApply"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:text="@string/button_apply" />
            </LinearLayout>

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- ===== RecyclerView (content) =====
         Renders daily summaries (adapter supplies top/bottom padding at runtime)
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="200dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- ===== Bottom Navigation ===== -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_gravity="bottom"
        android:background="@color/sand_50"
        app:itemIconTint="@color/bottom_nav_icon_tint"
        app:itemRippleColor="@android:color/transparent"
        app:itemTextColor="@color/bottom_nav_text_tint"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/menu_bottom" />

    <!-- ===== Center FAB (Add Mood) ===== -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAdd"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="40dp"
        app:srcCompat="@drawable/ic_add_24"
        app:tint="@color/colorSurface"
        app:backgroundTint="@color/colorTextPrimary"
        app:useCompatPadding="true"
        android:contentDescription="@string/button_addMood"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
