<?xml version="1.0" encoding="utf-8"?>
<!--
  Daily insights screen:
    • Date header with previous/next chevrons
    • Custom DailyMoodRingView visualizing counts + average label
    • Legend showing mood icons with counts
    • Same BottomNavigation + FAB as Home

  Bound by: InsightsActivity.kt
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sand_50">

    <!-- ===== Main insights card =====
         Large Material card to visually group all insights content
    -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/insightsCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="120dp"
        android:paddingStart="24dp"
        android:paddingTop="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="24dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="24dp"
        app:cardElevation="10dp"
        app:cardUseCompatPadding="true">

        <!-- Vertical stack: header → ring → average → legend -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:orientation="vertical">

            <!-- ===== Date header with chevrons =====
                 ConstraintLayout centers the date; chevrons align to its sides
            -->
            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="12dp">

                <!-- Previous chevron -->
                <ImageButton
                    android:id="@+id/btnPrevDay"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:layout_marginEnd="4dp"
                    android:background="@android:color/transparent"
                    android:contentDescription="@string/previous_day"
                    android:padding="0dp"
                    android:src="@drawable/ic_chevron_left_24"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toStartOf="@id/tvDate"
                    app:layout_constraintTop_toTopOf="parent"
                    app:tint="@color/colorAccent" />

                <!-- Centered date label (e.g., "Sunday, 19 Oct 2025")
                     Value set by InsightsActivity.renderDay()
                -->
                <TextView
                    android:id="@+id/tvDate"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/black"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

                <!-- Next chevron -->
                <ImageButton
                    android:id="@+id/btnNextDay"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:layout_marginStart="4dp"
                    android:background="@android:color/transparent"
                    android:contentDescription="@string/next_day"
                    android:padding="0dp"
                    android:src="@drawable/ic_chevron_right_24"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toEndOf="@id/tvDate"
                    app:layout_constraintTop_toTopOf="parent"
                    app:tint="@color/colorAccent" />
            </androidx.constraintlayout.widget.ConstraintLayout>


            <!-- ===== Mood ring visualization =====
                 Custom view draws distribution ring + center label
            -->
            <org.wit.mood.views.DailyMoodRingView
                android:id="@+id/moodRing"
                android:layout_width="320dp"
                android:layout_height="320dp" />

            <!-- ===== Average label =====
                 Shows the qualitative average (e.g., "Average: Relaxed 😌")
                 Text set in renderDay()
            -->

            <TextView
                android:id="@+id/tvAverage"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:textColor="@color/black"
                android:textSize="16sp"
                android:textStyle="italic" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnOpenPlaylist"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/button_open_playlist"
                app:icon="@drawable/ic_spotify"
                app:iconGravity="textStart"
                app:iconPadding="8dp" />


            <!-- ===== Legend (icons + counts + labels) =====
                 Flexbox wraps legend items across lines and centers them
                 Children are inflated from view_legend_item.xml in renderLegend()
            -->
            <com.google.android.flexbox.FlexboxLayout
                android:id="@+id/legend"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                app:alignItems="center"
                app:flexWrap="wrap"
                app:justifyContent="center"
                app:showDividerHorizontal="none"
                app:showDividerVertical="none" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- ===== Bottom navigation (same as Home) ===== -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_gravity="bottom"
        android:background="@color/sand_50"
        app:itemIconTint="@color/bottom_nav_icon_tint"
        app:itemRippleColor="@android:color/transparent"
        app:itemTextColor="@color/bottom_nav_text_tint"
        app:labelVisibilityMode="unlabeled"
        app:menu="@menu/menu_bottom" />

    <!-- ===== Center FAB (Add mood) ===== -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAdd"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="40dp"
        app:backgroundTint="@android:color/black"
        app:srcCompat="@drawable/ic_add_24"
        app:tint="@android:color/white"
        app:useCompatPadding="true"
        android:contentDescription="@string/button_addMood" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
